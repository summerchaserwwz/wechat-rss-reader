# 长程任务标准

本项目的长程任务必须先写 `long-running-task-contract.md`，明确：单一目标、范围、人工门禁、证据循环、暂停条件和停止条件。

固定循环：

```text
实现 -> 静态检查 -> 本机冒烟 -> reviewer -> 修复 -> 重跑证据 -> 更新 progress
```

需要人工完成的协议接受、登录、扫码、付费和时间性观察不会扩大 agent 权限。外部状态未满足时记录 blocker/residual，并保留可恢复下一步。
