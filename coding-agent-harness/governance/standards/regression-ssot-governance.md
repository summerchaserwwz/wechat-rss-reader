# Regression SSoT 治理

- Gate 定义和当前结果只写 `governance/regression/Regression-SSoT.md`。
- 何时触发哪些 Gate 只写 `Cadence-Ledger.md`。
- `fail`、`inconclusive`、`partial` 和 `skipped-with-reason` 必须有 residual、Owner 和重跑条件。
- 环境未满足时不得把静态检查升级描述为 L2/L3 通过。
- 秘密路径和原始日志不得作为公开 evidence。
