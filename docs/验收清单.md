# 验收清单

## 资格与抓取

- [x] 能以公众号/服务号运营者身份完成微信授权。
- [x] 12 个公众号已经加入 WeRSS。
- [x] 12/12 个公众号至少已有一篇文章。
- [x] 定时任务为 `17 * * * *`，状态启用。
- [ ] 至少观察 72 小时和 36 个自动周期。
- [ ] 7 天内至少一篇真实新文章自动进入 Readeck。

## 容器与端口

- [ ] `docker compose config --quiet` 通过。
- [ ] WeRSS 与 Readeck 均 healthy，两个 Caddy 均 running。
- [ ] WeRSS 只绑定 `127.0.0.1:8001`。
- [ ] RSS Caddy 只绑定 `127.0.0.1:8080`。
- [ ] Readeck 只绑定 `127.0.0.1:8002`。
- [ ] Readeck Caddy 只绑定 `127.0.0.1:8082`。
- [x] Readeck 容器原生运行在 `aarch64`。
- [ ] WeRSS 上游伪 ARM64 residual 已保留，没有伪装通过。
- [ ] Docker 日志限制为 10 MB × 3。

## Readeck 阅读链路

- [x] 90 篇完整正文已进入 Readeck。
- [x] 未登录首页跳转到登录页。
- [x] 未登录 `/api/bookmarks` 返回 `401`。
- [x] 收藏文章能进入 Obsidian。
- [x] Readeck 高亮同步为 Obsidian `==高亮==`。
- [x] 高亮批注进入摘录区和原文脚注。
- [x] 重复同步后人工笔记、hash 和 mtime 保持不变。
- [x] 同名文章使用日期、标题和短 ID 防冲突。

## Obsidian

- [x] 目标目录为 `02_Archive/02_DailyProcessed/reading/readeck_inbox`。
- [x] Base 过滤该目录并显示公众号、时间、状态、评分、主题和提升去向。
- [x] 真实长文、高亮、批注和“我的笔记”样本已生成。
- [ ] 在 Obsidian UI 打开 Base，确认列类型与排序。
- [ ] 补齐普通长文、多图文、表格/代码、复杂排版四类额外样本。
- [x] 标题、公众号、时间、原文链接正确，中文无乱码。
- [x] 原文留在 Archive，Knowledge/Output 只创建衍生条目。

## Cloudflare 外网

- [ ] 创建独立 `wechat-rss` Tunnel。
- [ ] `reader.sumerchaser.top` 指向本机 `127.0.0.1:8082`。
- [ ] 公网首页只跳转到 HTTPS 登录页。
- [ ] 公网未登录 API 返回 `401`。
- [ ] 公网 `/feed/all.atom` 返回 `404`，没有误暴露 WeRSS。
- [ ] LaunchAgent 开机自启且 Tunnel 有活动连接。

自动入口：

```bash
./scripts/verify.sh --local
./scripts/verify.sh --reader-public
```

## 备份与回滚

- [ ] `./scripts/backup.sh` 成功，归档权限为 `600`。
- [ ] 归档包含 WeRSS、Readeck、`.env`、API Token 和同步状态。
- [ ] Readeck 用户、文章、收藏和批注计数可恢复。
- [ ] `./scripts/restore-test.sh` 使用独立端口和 Compose project 通过。
- [ ] Cloudflare 已启用时，作用域 Tunnel 凭据包含在备份中，但账户 `cert.pem` 不包含。
- [ ] 旧镜像摘要可重新启动。

## 当前必须由用户完成的门禁

- 解锁 Mac，以便完成 Obsidian 实机截图。
- 在 Cloudflare 官方授权页批准 `cloudflared` 管理 `sumerchaser.top`，并允许创建 Tunnel/DNS。
- 等待 72 小时和 7 天的真实时间性证据。
